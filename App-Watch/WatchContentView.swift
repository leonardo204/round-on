import SwiftUI
import Shared

struct WatchContentView: View {
    var body: some View {
        Text("라운드온")
            .font(.system(size: 24, weight: .bold))
            .foregroundStyle(Color.winterTextPrimary)
    }
}
